#include <criterion.h>

Test(suite_name, test_name)
{
    // Your testing code here
    cr_assert(1 == 2, "description");  // The test will fail if 'expr' is false
    cr_assert(1 == 2, "description");  // The test will fail if 'expr' is false
}

